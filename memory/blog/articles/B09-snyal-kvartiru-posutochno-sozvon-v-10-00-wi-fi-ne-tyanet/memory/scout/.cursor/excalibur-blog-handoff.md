SCOUT BLOCK: MCP-KV Wordstat недоступен в текущем интерфейсе.

По контракту нельзя принимать переданные частоты как замену обязательному preflight `wordstat_get_user_info` и live-вызовам `wordstat_get_top_requests`. Поэтому `.cursor/excalibur-blog-handoff.md` не создаю и тему не финализирую.
