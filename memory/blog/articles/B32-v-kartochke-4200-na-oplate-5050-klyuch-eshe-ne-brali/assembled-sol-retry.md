# Sol inputs — B32 RETRY (gate: max 1050 words, NO h1, NO strong)

Return ONLY article.html body HTML. Start with `<p>` NOT `<h1>`. NO `<strong>`. Target **950–1050 words** (HARD max 1100).

Rewrite shorter version of drafts/writer.html — SOUL voice. Cut duplicate explanations; keep one checklist at end.

H1 text lives in title-brief only — do NOT output h1 tag.

Keep: identity line, «Нет. Так не заселяем.» once, hammer «Не X. Не Y. А Z.», lockpick question, «Сначала проверка. Потом перевод. Не наоборот.», «Наш вывод простой.», ONE CTA, 4 /blog/ links, mid TG/MAX question.

--- WRITER DRAFT ---
