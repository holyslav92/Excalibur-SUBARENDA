SCOUT BLOCK: в текущем окружении недоступен MCP-KV Wordstat (`wordstat_get_user_info`), поэтому обязательный preflight выполнить нельзя.

По контракту частоты нельзя принимать из входных данных или выдумывать. Handoff `.cursor/excalibur-blog-handoff.md` не создаю; Derouter не запускаю.
