WORDSTAT MCP BLOCKER: в текущем окружении недоступен `CallMcpTool` для обязательного solo-вызова `mcp-kv / wordstat_get_user_info` и live-проверки частот.

Handoff не создан: по контракту нельзя использовать переданные частоты вместо live MCP-KV и нельзя писать handoff-прозу без Derouter utility (`gpt-5.6-terra`).
