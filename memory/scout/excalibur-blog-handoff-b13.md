SCOUT BLOCK: в текущем окружении недоступны обязательные инструменты:

- MCP-KV Wordstat (`wordstat_get_user_info` preflight и live-проверки);
- Derouter utility `gpt-5.6-terra` для подготовки handoff.

Поэтому я не могу валидировать переданные частоты или создавать `.cursor/excalibur-blog-handoff.md`.
